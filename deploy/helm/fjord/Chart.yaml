# SPDX-License-Identifier: Apache-2.0

apiVersion: v2
name: fjord
description: |
  Experimental Kafka-compatible broker with log data in object storage and
  sequencing and metadata in a pluggable coordinator. The chart supports
  single-logical-broker and multi-broker evaluation topologies; validate
  compatibility and recovery behavior before deployment.
type: application
version: 0.1.4
appVersion: "0.1.4"
